@if (ViewBag.Heading?.Length > 0)
{
    <div class="subheader p-2">
        <h1 class="subheader-title">
            <a href="javascript:window.history.back()"><i class="fas fa-arrow-circle-left text-secondary"></i></a> @Html.Raw(ViewBag.Heading)
        </h1>
    </div>
}